
// java standard library imports
import java.util.List;

// external library imports
import processing.core.PImage;

public class EntityAPlus extends EntityBase
{
    // constructor
    public EntityAPlus(
        String id,
        Point position,
        List<PImage> images)
    {
        super(id, position, images);
    }
}